[[permission]]
identifier = "allow-migration-commands"
description = "Enables read-only migration status inspection from the frontend. Destructive run/rollback commands are intentionally NOT exposed — the frontend never drives schema migrations."
commands.allow = [
    "cmd_get_migration_status",
    "cmd_list_migrations"
]

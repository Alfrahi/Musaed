# Model Library Feature

Manages local AI models using Ollama.

## Components

- `ModelLibrary`: Main interface for browsing and managing models.
- `ModelCard`: Display and actions for a single model.
- `ModelSelector`: Dropdown for switching models in chat.

## Hooks

- `useModelActions`: Handles pulling, deleting, and listing models via IPC.

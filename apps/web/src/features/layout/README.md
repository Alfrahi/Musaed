# Layout Feature

Core UI components that wrap the application.

## Components
- `DirectionProvider`: Manages LTR/RTL switching based on language.
- `ErrorBoundary`: Catch-all for React rendering errors.
- `ToastProvider`: Management of system notifications.
